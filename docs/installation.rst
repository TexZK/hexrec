============
Installation
============

From PIP
--------

At the command line:

.. code-block:: sh

    $ pip install hexrec


From source
-----------

At the command line, at the root of the source directory:

.. code-block:: sh

    $ python setup.py install
