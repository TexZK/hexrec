============
Installation
============

From PIP
--------

At the command line::

    pip install hexrec


From source
-----------

At the command line, at the root of the source directory::

    python setup.py install
